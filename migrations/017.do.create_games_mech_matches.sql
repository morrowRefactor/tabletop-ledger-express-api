CREATE TABLE games_mech_matches (
    id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    mech_id INTEGER 
        REFERENCES games_mech(mech_id) ON DELETE CASCADE NOT NULL,
    game_id INTEGER
        REFERENCES games(id) ON DELETE CASCADE NOT NULL
);