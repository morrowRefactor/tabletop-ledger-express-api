CREATE TABLE games_cat_matches (
    id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    cat_id INTEGER 
        REFERENCES games_cat(cat_id) ON DELETE CASCADE NOT NULL,
    game_id INTEGER
        REFERENCES games(id) ON DELETE CASCADE NOT NULL
);